[pytest]
pythonpath = .
testpaths = backend/tests
asyncio_mode = auto
markers =
    postgres: tests requiring a reachable PostgreSQL database with pgvector
    real_models: tests requiring the configured local embedding and reranker models
